# NIXPACKS Configuration for Hospital Management System Backend
# Forces npm install instead of npm ci

[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm install --no-audit --no-fund --prefer-offline"]

[start]
cmd = "node --max-old-space-size=512 --expose-gc server.js" 